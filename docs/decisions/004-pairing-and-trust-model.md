# 004 配对与信任模型

## 背景

局域网默认不可信。只有做清晰的首次配对与信任持久化，后续输入和剪贴板开放才有边界。

## 决策

- 每台设备首次启动生成独立设备身份和本地证书
- 首次连接时展示双方指纹，要求用户确认
- 配对成功后将设备元数据与证书指纹写入本地信任库
- 未受信设备不得建立可用会话
- 证书变化视为新设备，需要重新配对
- 撤销信任后旧会话立即失效

## 结果

- 安全边界清晰，符合“默认不信任局域网”的原则
- 指纹确认交互会增加首次连接成本，但可接受
- 证书轮换和重装场景有明确处理规则
