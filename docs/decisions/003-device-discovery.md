# 003 设备发现策略

## 背景

自动发现是体验增强，但局域网环境差异大，不能把连接能力完全建立在发现成功之上。

## 决策

- MVP 支持局域网自动发现
- 自动发现采用本地网络服务广播/发现机制实现
- 手工输入 IP 或主机名添加是必备能力，不依赖自动发现
- 发现结果至少展示设备名、平台、地址、连接状态、受信状态

## 结果

- 自动发现提升体验，但不会成为连接阻塞点
- 手工添加保证受限网络环境下仍能使用
- 会话层必须支持“发现入口”和“手工入口”两套接入路径
